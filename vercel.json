{
  "buildCommand": "cd frontend && npm install && NODE_NO_WARNINGS=1 npm run build",
  "installCommand": "NODE_NO_WARNINGS=1 npm --prefix frontend install",
  "outputDirectory": "frontend/build",
  "framework": "create-react-app",
  "env": {
    "NODE_NO_WARNINGS": "1"
  }
}
